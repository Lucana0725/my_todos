<h1>Profile Edit</h1>

<%= form_with model: @user, url: user_path(@user.id), method: :patch do |f| %>
  <%= f.label :nickname, "ニックネーム" %>
  <%= f.text_field :nickname %>

  <%= f.submit "編集を保存" %>
<% end %>

<%= link_to "戻る", user_path(current_user) %>
<%= link_to "退会確認画面", unsubscribe_path %>